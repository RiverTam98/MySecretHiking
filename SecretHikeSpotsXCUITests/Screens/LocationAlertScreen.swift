
import Foundation
import XCTest

class LocationPermissionDialogScreen: Screen {
    
    static let locationPermissionDialogHeading: XCUIElement = staticTexts["Allow “SecretHikeSpots” to use your location?"]
    static let locationPermissionDialogAllowOnceButton: XCUIElement = buttons["Allow Once"]
    
}
